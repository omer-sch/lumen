# UI bug log

> Populated by Step 6 (visual regression + bug-hunt sweep). Entries
> documented as one line each with the spec path, the page, and a short
> description. Bugs are triaged here, not fixed in test passes.

## Status

Step 5 (security E2E additions) and Step 6 (visual regression) have not run
yet. This file will be populated when those tiers execute. The existing
Playwright E2E suite (29 passing, 10 skipped, 21 not reached) does not
currently report any failing assertions; nothing to log.

## Entry format (for future passes)

```
- [page] [short bug summary] — captured by [spec path]:[line]; screenshot
  ref: [tests/e2e/visual/__screenshots__/...]
```
