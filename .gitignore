# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
# Playwright authenticated storage state — never commit session tokens.
/tests/.auth/
/test-results/
/playwright-report/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — block all .env variants, allow only the example.
.env
.env.*
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local agent memory (file-based MVP store)
/data/

# Local BQ schema-discovery dumps — exploration artifacts, not source.
/tmp/
